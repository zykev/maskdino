_BASE_: default_maskdino_orth_swin_scratch_config.yaml

MODEL:
  BACKBONE:
    # This switch is the only source of image-encoder initialization.
    # MODEL.WEIGHTS remains reserved for a complete MaskDINO checkpoint.
    USE_PRETRAINED: true
    PRETRAINED_FORMAT: "swin_mae"
    PRETRAINED_WEIGHTS: "/home/zychen/Documents/intraoral_code/Swin-MAE/exp/swinmae_v1/checkpoint-400.pth"
    PRETRAINED_CHECKPOINT_KEY: "model"
WANDB:
  NAME: "maskdino_orth_swin_mae"

OUTPUT_DIR: output/maskdino_orth_swin_mae
